# ─────────────────────────────────────────────────────────────────────────────
# Tundra Connection — Frontend env vars (FASE 11)
#
# Copiar a .env.local (dev) o configurar en Vercel → Project → Settings → Env.
# Vite expone solo variables con prefijo VITE_ al cliente.
# ─────────────────────────────────────────────────────────────────────────────

# Backend público — sin "/" final.
# Local: http://localhost:8000
# Prod (Railway): https://tundra-backend.up.railway.app
VITE_API_URL=http://localhost:8000
